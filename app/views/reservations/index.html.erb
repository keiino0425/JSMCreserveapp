<div class="container">
  <div class="row">
    <div class="col-12 text-center">
      <h1><%= @teacher.teacher_name %>先生仮予約画面</h1>
      <p>1ヶ月先まで仮予約することができます。</p>
    </div>
    <div class="col-12 mt-3">
      <%= week_calendar events: @reservations do |date, reservations| %>
        <%= date.day %>
      <% end %>
    </div>
  </div>
</div>